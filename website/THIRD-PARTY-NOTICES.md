# Third-party notices

The Fabric OS website directly depends on the following packages. Each is distributed under the MIT License as declared by its package metadata:

- React — MIT
- React DOM — MIT
- React Router DOM — MIT
- Vite — MIT
- Express — MIT
- node-postgres (`pg`) — MIT
- `@vitejs/plugin-react` — MIT

Transitive dependencies are governed by their own license files in `node_modules` and the lockfile resolution. Before a release, generate and archive a complete SBOM and transitive license report for the exact build, and review any dependency added after this notice was written.
