[package]
name = "libipc"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Authenticated Unix-socket IPC (line-delimited JSON) with peer credentials"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }

[lints.rust]
unsafe_code = "allow"
