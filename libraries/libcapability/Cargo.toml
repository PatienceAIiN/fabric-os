[package]
name = "libcapability"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Least-privilege, scoped, revocable capability model for ai-native-os"

[dependencies]

[lints.rust]
unsafe_code = "forbid"
