[package]
name = "libmodel"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Local model management: manifests, checksum/trust verification, lifecycle"

[dependencies]
libcrypto = { path = "../libcrypto" }
serde = { workspace = true }
serde_json = { workspace = true }

[lints.rust]
unsafe_code = "forbid"
