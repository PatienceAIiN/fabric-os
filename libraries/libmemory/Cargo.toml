[package]
name = "libmemory"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "OS-managed AI memory: classes, ownership, access control, retention"

[dependencies]
libcrypto = { path = "../libcrypto" }
serde = { workspace = true }
serde_json = { workspace = true }

[lints.rust]
unsafe_code = "forbid"
