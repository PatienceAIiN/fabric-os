[package]
name = "libprovider"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Provider-independent AI interface; Local and Claude providers"

[dependencies]
libcredentials = { path = "../libcredentials" }
serde = { workspace = true }
serde_json = { workspace = true }

[lints.rust]
unsafe_code = "forbid"
