[package]
name = "libbroker"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Agent broker: capability delegation, revocation, and multi-agent message envelope"

[dependencies]
libcapability = { path = "../libcapability" }
serde = { workspace = true }
serde_json = { workspace = true }

[lints.rust]
unsafe_code = "forbid"
